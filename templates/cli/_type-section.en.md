## Project Type: CLI Tool

```
src/
├── commands/   # command definitions (1 command = 1 file)
├── handlers/   # actual command logic
└── main.*      # entry point (argument parsing only)
```

Type-specific rules:

@.claude/rules/cli.md
