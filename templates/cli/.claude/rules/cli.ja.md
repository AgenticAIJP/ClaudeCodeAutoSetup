# CLIツール型ルール

- 1コマンド = 1ファイル(`src/commands/`)。main に処理を書かない
- 実処理は handlers に置き、commands からは呼び出すだけにする
- ユーザー向けメッセージとエラーは stderr、パイプ可能な出力は stdout に分ける
- 終了コードを正しく返す: 成功 0 / ユーザーエラー 1 / 内部エラー 2
- `--help` の内容はコマンド追加と同じ変更で更新する
- 破壊的な操作(削除・上書き)には確認プロンプトか `--force` を必須にする
