# アーキテクチャルール

- ビジネスロジックは `src/core/` に置く。エントリポイントに直接書かない
- 依存方向を守る: エントリポイント → core → utils。逆方向の import は禁止
- 「動くから」という理由で層を飛ばす近道をしない
- 新しい外部ライブラリを追加する前に、必ず理由を説明して承認を得る
- 公開インターフェース(関数シグネチャ・API)を変える場合は、影響範囲を先に列挙する
