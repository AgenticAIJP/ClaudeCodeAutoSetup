## プロジェクトタイプ: AI Agent型(MCP・自動化)

```
agents/     # エージェント定義(役割・プロンプト)
skills/     # スキル(再利用可能な能力単位)
tools/      # ツール実装(外部APIとの接続等)
```

タイプ固有ルール:

@.claude/rules/agent.md

MCP サーバーの設定例はルートの `.mcp.sample.json` を参照。
