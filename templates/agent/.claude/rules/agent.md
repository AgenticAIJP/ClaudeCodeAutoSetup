# AI Agent型ルール

- 1エージェント = 1責務。「なんでもできるエージェント」を作らない
- ツールの入出力はスキーマ(型)を必ず定義する。any / dict のまま渡さない
- 外部 API を呼ぶツールは必ずタイムアウトとリトライ方針を持つ
- エージェントのプロンプトはコードに埋め込まず `agents/` 配下のファイルに置く
- 秘密情報(APIキー)は環境変数から読む。コード・プロンプトに直書き禁止
- エージェントの動作テストは実 API ではなくモックで行い、実 API テストは明示的に分離する
