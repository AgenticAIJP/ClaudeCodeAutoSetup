## Project Type: AI Agent (MCP / automation)

```
agents/     # agent definitions (roles, prompts)
skills/     # skills (reusable capability units)
tools/      # tool implementations (external API connections etc.)
```

Type-specific rules:

@.claude/rules/agent.md

For MCP server config examples, see `.mcp.sample.json` in the root.
