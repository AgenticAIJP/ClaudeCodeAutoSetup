# APIサービス型ルール

- routes は薄く保つ: 検証・変換のみ。ビジネスロジックは services へ
- DB アクセスは models(または repository)経由のみ。services から生クエリを書かない
- エンドポイントを追加・変更したら、同じ変更で `docs/API_SPEC.md` を更新する
- 破壊的変更(レスポンス形式の変更等)は必ずバージョニング(/v1, /v2)で行う
- すべての入力を検証する。「フロントが検証しているから」を理由にしない
- エラーレスポンスの形式はプロジェクトで統一する
