# API仕様書

> ⚠️ エンドポイントを追加・変更したら、必ずこのファイルも同じ変更で更新すること。

## 共通仕様

- ベースURL: `/api/v1`
- 認証: <!-- TODO: Bearer トークン等 -->
- エラーレスポンス形式:

```json
{ "error": { "code": "STRING", "message": "STRING" } }
```

## エンドポイント一覧

### GET /health

ヘルスチェック。

- レスポンス: `200 OK` `{ "status": "ok" }`

<!-- TODO: エンドポイントを追加していく -->
