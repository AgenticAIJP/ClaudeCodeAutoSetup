## Project Type: API Service

```
src/
├── routes/     # routing (keep thin)
├── services/   # business logic
└── models/     # data models
docs/
├── API_SPEC.md # API spec (always kept current)
└── DATABASE.md # DB design
```

Type-specific rules:

@.claude/rules/api.md
